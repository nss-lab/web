# For local preview only — GitHub Pages builds the site for you on push.
# Local: `cd docs && bundle install && bundle exec jekyll serve`
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8" # required for local serve on Ruby 3+
